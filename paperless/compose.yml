services:
  paperless:
    image: ghcr.io/paperless-ngx/paperless-ngx:3.0.5@sha256:65a4cabf0169ea7fbd90ab7bb28ba3f8b5909613635acda1a03ad606f34b456b
    restart: always
    deploy:
      resources:
        limits:
          memory: '4G'
          cpus: '4.0'
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      PAPERLESS_REDIS: redis://redis:6379
      PAPERLESS_DBENGINE: postgresql
      PAPERLESS_DBHOST: db
      PAPERLESS_DBNAME: db
      PAPERLESS_DBUSER: user
      PAPERLESS_DBPASS: password
      PAPERLESS_OCR_LANGUAGE: deu+eng
      PAPERLESS_OCR_LANGUAGES: 'eng deu'
      PAPERLESS_URL: https://${DOMAIN}
      PAPERLESS_CSRF_TRUSTED_ORIGINS: https://${DOMAIN}
      PAPERLESS_ALLOWED_HOSTS: ${BASE_DOMAIN}
      PAPERLESS_USE_X_FORWARD_HOST: true
      PAPERLESS_TIME_ZONE: Europe/Berlin
      PAPERLESS_OCR_USER_ARGS: '{"invalidate_digital_signatures": true}'
      PAPERLESS_APPS: 'allauth.socialaccount.providers.openid_connect'
      PAPERLESS_SOCIALACCOUNT_PROVIDERS: '${SOCIALACCOUNT_PROVIDERS}'
      PAPERLESS_DISABLE_REGULAR_LOGIN: ${DISABLE_REGULAR_LOGIN}
      PAPERLESS_SECRET_KEY: ${SECRET_KEY}
      PAPERLESS_CONSUMER_DELETE_DUPLICATES: true
      PAPERLESS_TRUSTED_PROXIES: ${TRUSTED_PROXIES}
    volumes:
      - ${DIRECTORY}/data:/usr/src/paperless/data
      - ${DIRECTORY}/export:/usr/src/paperless/export
      - ${DIRECTORY}/media:/usr/src/paperless/media
      - ${DIRECTORY}/consume:/usr/src/paperless/consume
    labels:
      - backup.target=${BACKUP_TARGET:-paperless}
      - net.unraid.docker.managed=dockhand
      - net.unraid.docker.webui=https://${DOMAIN}
      - net.unraid.docker.icon=https://cdn.jsdelivr.net/gh/selfhst/icons/webp/paperless-ngx.webp
      - traefik.enable=true
      - traefik.http.routers.paperless.rule=Host(`${DOMAIN}`)
      - traefik.http.routers.paperless.entrypoints=https
      - traefik.http.routers.paperless.tls=true
      - traefik.http.routers.paperless.tls.certresolver=desec
    expose:
      - 8000
    networks:
      - proxy
      - net

  db:
    image: postgres:17.10@sha256:7958605b474b3d264a969cb3a123d6aa00ad1e1fe9da8a69984dabb704d93317
    restart: always
    deploy:
      resources:
        limits:
          memory: '1G'
          cpus: '1.0'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U user -d db']
      interval: 5s
      timeout: 5s
      retries: 5
    environment:
      POSTGRES_DB: db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - db:/var/lib/postgresql/data
    labels:
      - net.unraid.docker.managed=dockhand
      - net.unraid.docker.icon=https://cdn.jsdelivr.net/gh/selfhst/icons/webp/postgresql.webp
    expose:
      - 5432
    networks:
      - net

  redis:
    image: redis:8.10.0@sha256:a7efd4ed5037dcfe1543c0e50b9f06293d3e88827005544bf37f381eea5f01cf
    restart: always
    deploy:
      resources:
        limits:
          memory: '256M'
          cpus: '0.5'
    healthcheck:
      test: ['CMD-SHELL', 'redis-cli ping || exit 1']
    volumes:
      - redis:/data
    labels:
      - net.unraid.docker.managed=dockhand
      - net.unraid.docker.icon=https://cdn.jsdelivr.net/gh/selfhst/icons/webp/redis.webp
    expose:
      - 6379
    networks:
      - net

networks:
  proxy:
    external: true
  net:
    external: false

volumes:
  db: {}
  redis: {}
