services:
  godash:
    image: ghcr.io/flohoss/godash:v1.3.0@sha256:5441b67a4d67ed5cc972a96631936da501bd98de1290e40a391d9d930bbca683
    restart: always
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M
          cpus: '0.5'
    environment:
      PUID: 1000
      PGID: 1000
    volumes:
      - ${DIRECTORY}:/app/config
    labels:
      - net.unraid.docker.managed=dockhand
      - net.unraid.docker.webui=https://${DOMAIN}
      - net.unraid.docker.icon=https://cdn.jsdelivr.net/gh/selfhst/icons/webp/homebox.webp
      - traefik.enable=true
      - traefik.http.routers.godash.entrypoints=https
      - traefik.http.routers.godash.rule=Host(`${DOMAIN}`)
      - traefik.http.routers.godash.tls=true
      - traefik.http.routers.godash.tls.certresolver=desec
    expose:
      - 8156
    networks:
      - proxy

networks:
  proxy:
    external: true
